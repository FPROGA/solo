/*import { MinLength, Matches, IsPhoneNumber } from 'class-validator';

export class UserLogin {
  @IsPhoneNumber()
  phoneNumber: string;
  @MinLength(8, { message: 'Минимум 8 символов' })
  @Matches(/^(?=.*[a-z])(?=.*\d).+$/, {
    message: 'Пароль должен содержать буквы и цифры',
  })
  password: string;
}
*/
